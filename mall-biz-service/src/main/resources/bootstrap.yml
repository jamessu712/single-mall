spring:
  cloud:
    config:
      uri: http://localhost:8888
  application:
    name: biz-service
server:
  port: 8081
---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888
