spring:
  cloud:
    config:
      uri: http://localhost:8888
    loadbalancer:
      ribbon:
        enabled: false
  application:
    name: web-service
server:
  port: 8082

---
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-server:8888
